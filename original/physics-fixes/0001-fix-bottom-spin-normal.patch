--- a/collide.c
+++ b/collide.c
@@ -1280,9 +1280,9 @@
     p[a].vel.y = muw * p[a].vel.y;
 #endif
 #else
-    rabhat.x=-1*p[b].norm.x; 
-    rabhat.y=-1*p[b].norm.y; 
-    rabhat.z=-1*p[b].norm.z; 
+    rabhat.x=p[b].norm.x;
+    rabhat.y=p[b].norm.y;
+    rabhat.z=p[b].norm.z;
     vt.z=0;
     vt.x=-p[a].vel.x;
     vt.y=-p[a].vel.y;
