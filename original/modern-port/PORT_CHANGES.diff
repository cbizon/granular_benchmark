diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/cell.cc b/cell.cc
--- a/cell.cc	2026-07-15 13:28:04
+++ b/cell.cc	2026-07-15 18:57:28
@@ -1,5 +1,6 @@
 #include <stdlib.h>
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <stdio.h>
 #include <assert.h>
 #include "cell.h"
@@ -23,7 +24,7 @@
 } */
 
 
-CellSet::add(int item) {
+void CellSet::add(int item) {
   if (CellContents == NULL) {
     CellContents = new CellNodeType;
     if (CellContents == NULL) {
@@ -57,7 +58,7 @@
   iNumMembers += 1;
 }
 
-CellSet::display(char *str) {
+void CellSet::display(char *str) {
   CellNodePtr TmpPtr = CellContents;
 /*  int i = 0;
   while (TmpPtr != NULL) {
@@ -72,7 +73,7 @@
     sprintf(str,"%40s","fuck ");
 }
 
-CellSet::display() {
+void CellSet::display() {
   CellNodePtr TmpPtr = CellContents;
   while (TmpPtr != NULL) {
     if (TmpPtr->Valid)
@@ -127,7 +128,7 @@
   }
 }*/
 
-CellSet::remove(int item) {
+void CellSet::remove(int item) {
   CellNodePtr OldPtr = CellContents;
   CellNodePtr NewPtr = CellContents;
   int NotFound = 1;
@@ -151,7 +152,7 @@
   }
 }
 
-CellSet::removeall() {
+void CellSet::removeall() {
   CellNodePtr TmpPtr = CellContents;
   while (TmpPtr != NULL) {
     TmpPtr->Valid = 0;
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/cell.h b/cell.h
--- a/cell.h	2026-07-15 13:28:04
+++ b/cell.h	2026-07-15 18:57:28
@@ -15,14 +15,13 @@
   CellNodePtr CellContents;
 public:
   CellSet();
-  add(int item);
-  remove(int item);
-  removeall();
+  void add(int item);
+  void remove(int item);
+  void removeall();
   int members(int *TheMembers);
-  display();
-  display(char *str);
+  void display();
+  void display(char *str);
 };
-
 
 
 
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/clist.c b/clist.c
--- a/clist.c	2026-07-15 13:28:04
+++ b/clist.c	2026-07-16 10:26:12
@@ -6,7 +6,8 @@
 #include "detect.h"
 #include "files.h"
 #include <stdio.h>
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <stdlib.h>
 
 extern P_DATA p[];
@@ -19,6 +20,12 @@
            lastB;
 extern ParamStructPtr TheParams;
 
+static inline int grid_cell_exists(int x, int y, int z) {
+  return x >= 0 && x < XGSIZE
+      && y >= 0 && y < YGSIZE
+      && z >= 0 && z < ZGSIZE;
+}
+
 /* cl_calc()
 */
 
@@ -43,7 +50,7 @@
 #endif
 }
 
-int c_calc(int a, int debug) {
+void c_calc(int a, int debug) {
   double ct;
   int NumNeighbors,i,x,y,z,xcell,ycell,zcell;
   int xc,yc,zc;
@@ -76,6 +83,8 @@
         if (zc > ZBSIZE)
            zc=1;
        #endif /*PERIODIC in Z*/
+        if (!grid_cell_exists(xc,yc,zc))
+          continue;
 	NumNeighbors = TheGrid[xc][yc][zc].members(TheNeighbors);
         if (NumNeighbors == -99 ){
           fprintf(stdout,"Death in c_calc: %d, %d, %d\n",xcell+x,ycell+y,zcell+z);
@@ -195,7 +204,7 @@
       }
     }
     
-    if (min > -1) {
+    if (minb > -1) {
       c_add(a,minb,min);
     }
 
@@ -209,7 +218,7 @@
 }
 
 /*cw_calc: builds collision list for the moving wall*/
-int cw_calc(int debug){   
+void cw_calc(int debug){   
   double ct;
   printf("Calculating Bottom collisions at %f\n",Gtime);
   int NumNeighbors,i,x,y,z;
@@ -263,7 +272,7 @@
     c_add(TheParams->lwall,TheParams->lvwall,ct);}*/
 }
 
-int cv_calc(int a, int b, int debug) {
+void cv_calc(int a, int b, int debug) {
   double ct;
   int NumNeighbors,i,x,y,z,xcell,ycell,zcell;
   int xc,yc,zc,chec=0;
@@ -301,6 +310,8 @@
            zc=1;
 #endif
 #endif
+        if (!grid_cell_exists(xc,yc,zc))
+          continue;
 	NumNeighbors = TheGrid[xc][yc][zc].members(TheNeighbors);
         if (NumNeighbors == -99 ){
           fprintf(stdout,"1 Death in cv_calc: %d, %d, %d\n",xcell+x,ycell+y,zcell+z);
@@ -341,6 +352,8 @@
            zc=1;
 #endif
 #endif
+        if (!grid_cell_exists(xc,yc,zc))
+          continue;
 	NumNeighbors = TheGrid[xc][yc][zc].members(TheNeighbors);
         if (NumNeighbors == -99){
           fprintf(stdout,"2 Death in cv_calc: %d, %d, %d\n",xcell+x,ycell+y,zcell+z);
@@ -383,6 +396,8 @@
         }
 #endif
 #endif
+        if (!grid_cell_exists(xc,yc,zc))
+          continue;
 	NumNeighbors = TheGrid[xc][yc][zc].members(TheNeighbors);
     /*   if (chec)
            fprintf(stdout,"found %i neighbors in %i %i %i\n",NumNeighbors,xc,yc,zc);
@@ -430,6 +445,8 @@
            zc=1;
 #endif /*PERIODIC == 3*/
 #endif /*PERIODIC*/
+        if (!grid_cell_exists(xc,yc,zc))
+          continue;
 	NumNeighbors = TheGrid[xc][yc][zc].members(TheNeighbors);
     /*   if (chec)
            fprintf(stdout,"found %i neighbors in %i %i %i\n",NumNeighbors,xc,yc,zc);
@@ -480,6 +497,8 @@
            zc=1;
 #endif
 #endif
+        if (!grid_cell_exists(xc,yc,zc))
+          continue;
 	NumNeighbors = TheGrid[xc][yc][zc].members(TheNeighbors);
          if (NumNeighbors == -99){
           fprintf(stdout,"5 Death in cv_calc: %d %d %d\n",xcell+x,ycell+y,zcell+z);
@@ -525,6 +544,8 @@
            zc=1;
 #endif
 #endif
+        if (!grid_cell_exists(xc,yc,zc))
+          continue;
 	NumNeighbors = TheGrid[xc][yc][zc].members(TheNeighbors);
         if (NumNeighbors == -99){
           fprintf(stdout,"6 Death in cv_calc: %d %d %d\n",xcell+x,ycell+y,zcell+z);
@@ -627,7 +648,7 @@
 
 }
 
-int cs_calc(int a) {
+void cs_calc(int a) {
   if (a == TheParams->fstat)
     c_add(a,a,Gtime + TimeStep);
   else
@@ -647,7 +668,7 @@
 /* cl_print()
 */
 
-int cl_print( void ) {
+void cl_print( void ) {
   printf("void cl_print(): printing current collision list\n"); 
   printf("%-8s%8s%10s%5s%5s%5s\n","part ","event","time","a","b","cb");
   printf("%70s\n",SEP70);
@@ -710,7 +731,7 @@
 Adds a collision to a local collision list
 */
 
-int c_add( int a, int b, double t_c ) {
+void c_add( int a, int b, double t_c ) {
 
   C_DATA *tmp = (C_DATA *) calloc(1,sizeof(C_DATA));
   if (tmp == NULL) {
@@ -767,7 +788,7 @@
 
 
 /* deletes the first collision from p[a]'s collision list */
-int c_delete( int a ) {
+void c_delete( int a ) {
   C_DATA *toDelete, *restList;
   restList = p[a].cl->cnext;
   toDelete = p[a].cl;
@@ -779,7 +800,7 @@
 //Originally, the if covered to past todel = NULL:, but I am going to
 //move it
 
-int destroy_list( C_DATA *todel ) {
+void destroy_list( C_DATA *todel ) {
   if (todel->cnext != NULL) {
     destroy_list( todel->cnext );}
     free( todel );
@@ -787,7 +808,7 @@
  // }
 }
 
-int lel_destroy( int a, int debug ) {
+void lel_destroy( int a, int debug ) {
   if (p[a].cl != NULL) {
     destroy_list( p[a].cl );
     p[a].cl = NULL;
@@ -796,7 +817,7 @@
     fprintf(stderr,"lel_destroy: just destroyed list %d\n",a);
 }
 
-int lel_destroy_all() {
+void lel_destroy_all() {
   for(int i=0;i<NP;i++)
     lel_destroy(i,0);
 }
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/clist.h b/clist.h
--- a/clist.h	2026-07-15 13:28:04
+++ b/clist.h	2026-07-15 19:09:09
@@ -1,14 +1,14 @@
 void cl_calc(int);
-int c_calc(int,int);
-int cw_calc(int);
-int cv_calc(int,int,int);
-int cs_calc(int);
-int cl_print(void);
+void c_calc(int,int);
+void cw_calc(int);
+void cv_calc(int,int,int);
+void cs_calc(int);
+void cl_print(void);
 void cl_sort(void);
-int c_add(int,int,double);
+void c_add(int,int,double);
 int c_valid(int,int);
-int c_delete(int);
+void c_delete(int);
 int GetNextCollision();
-int destroy_list(C_DATA*);
-int lel_destroy(int,int);
-int lel_destroy_all();
+void destroy_list(C_DATA*);
+void lel_destroy(int,int);
+void lel_destroy_all();
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/collide.c b/collide.c
--- a/collide.c	2026-07-15 13:28:04
+++ b/collide.c	2026-07-16 12:09:37
@@ -8,7 +8,8 @@
 #include "files.h"
 #include "collide.h"
 #include "plist.h"
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <stdio.h>
 #include <stdlib.h>
 #include <time.h>
@@ -73,8 +74,8 @@
 extern double pzzflux[ZGSIZE];
 extern double pyzflux[ZGSIZE];
 extern double pzyflux[ZGSIZE];
-extern double loss[ZBSIZE];
-extern double gain[ZBSIZE];
+extern double loss[ZGSIZE];
+extern double gain[ZGSIZE];
 extern double vzbar[ZGSIZE];
 extern int listarray[NMOV];
 #if PERIODIC == 3
@@ -97,6 +98,18 @@
 extern FILE *gs;
 #endif
 
+static inline int allocated_grid_cell_exists(int x, int y, int z) {
+  return x >= 0 && x < XGSIZE
+      && y >= 0 && y < YGSIZE
+      && z >= 0 && z < ZGSIZE;
+}
+
+static inline int physical_grid_cell_exists(int x, int y, int z) {
+  return x >= 1 && x <= XBSIZE
+      && y >= 1 && y <= YBSIZE
+      && z >= 1 && z <= ZBSIZE;
+}
+
 inline double dot(PVECTOR a, PVECTOR b){
   return(a.x*b.x+a.y*b.y+a.z*b.z);
 }
@@ -671,7 +684,7 @@
 
 */
 
-int ballwall(int a, int debug, int *real) {
+void ballwall(int a, int debug, int *real) {
  int zp;
  double mag,angle;
  FILE *somebefore,*someafter;
@@ -1369,7 +1382,7 @@
 
 }
 
-int statstat(int a) {
+void statstat(int a) {
   static float countf=0.;
   static int firsttime = 2;
   int i,j,NumNeighbors,x,y,z,old;
@@ -2237,7 +2250,7 @@
 
 /* updates particle from its current time to newtime */
 
-int evolve(int a,double newtime,int debug) {
+void evolve(int a,double newtime,int debug) {
 
 #ifdef GRAVDIM
   double delt1=newtime-p[a].lgt;
@@ -2349,7 +2362,7 @@
 */
 }
 
-int g_evolve(double time,int debug) {
+void g_evolve(double time,int debug) {
   for(int i=0;i<TheParams->nball;i++)
     if (debug)
       evolve(i,time,1);
@@ -2357,7 +2370,7 @@
       evolve(i,time,0);
 }
 
-int vwall(int a,int debug) 
+void vwall(int a,int debug) 
 {
 
 #ifdef GRAVDIM
@@ -2375,6 +2388,12 @@
   int b = p[a].cl->b;
   c_delete(a);
   int xcell = p[a].cell.x, ycell = p[a].cell.y, zcell = p[a].cell.z;
+  if (!allocated_grid_cell_exists(xcell,ycell,zcell)) {
+    fprintf(stdout,"Death in vwall before grid access\n");
+    fprintf(stdout,"Particle %d is in cell %d,%d,%d. Gtime=%f\n",a,xcell,ycell,zcell,Gtime);
+    bomb(4,a);
+    return;
+  }
 /*  if(!((p[a].cell.x <= XBSIZE) && (p[a].cell.y <= YBSIZE) && (p[a].cell.z <= ZBSIZE))){
      fprintf(stdout,"Death in vwall\n");
      fprintf(stdout,"Particle %d is fucked.  Its cell is %d,%d,%d. Gtime=%f\n",a,p[a].cell.x,p[a].cell.y,p[a].cell.z,Gtime);
@@ -2489,13 +2508,19 @@
   }
 
 
+  if (!physical_grid_cell_exists(p[a].cell.x,p[a].cell.y,p[a].cell.z)) {
+    fprintf(stdout,"Death in vwall after virtual-cell crossing\n");
+    fprintf(stdout,"Particle %d escaped to cell %d,%d,%d. Gtime=%f\n",a,p[a].cell.x,p[a].cell.y,p[a].cell.z,Gtime);
+    bomb(4,a);
+    return;
+  }
   TheGrid[p[a].cell.x][p[a].cell.y][p[a].cell.z].add(a);
 
 
 }
 
 
-int vwallideal(int a) 
+void vwallideal(int a) 
 {
   double t_c;
   int b = p[a].cl->b;
@@ -3070,4 +3095,3 @@
 
 
 #endif
-
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/collide.h b/collide.h
--- a/collide.h	2026-07-15 13:28:04
+++ b/collide.h	2026-07-15 19:09:33
@@ -1,11 +1,11 @@
 double dot(PVECTOR,PVECTOR);
 double ballball(int,int);
-int ballwall(int,int,int*);
+void ballwall(int,int,int*);
 PVECTOR cross(PVECTOR,PVECTOR);
-int statstat(int);
-int vwall(int,int);
-int evolve(int,double,int);
-int g_evolve(double,int);
+void statstat(int);
+void vwall(int,int);
+void evolve(int,double,int);
+void g_evolve(double,int);
 double gasdev(void);
 double zdev(void);
 void thermalize(int,int);
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/detect.c b/detect.c
--- a/detect.c	2026-07-15 13:28:04
+++ b/detect.c	2026-07-15 19:22:16
@@ -2,7 +2,8 @@
 #include "main.h"
 #include "files.h"
 #include <stdlib.h>
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <stdio.h>
 #include <math.h>
 #include <assert.h>
@@ -947,6 +948,8 @@
       xh=rts;
   }
   fprintf(stdout,"Convergence on root of pos is sucky -- 1.\n");
+  bomb(0,a);
+  return HUGE;
 }
 
 double findvroot(int a,int w,double lbound,double rbound) {
@@ -1035,6 +1038,8 @@
       xh=rts;
   }
   fprintf(stdout,"Convergence on root is sucky -- 2.\n");
+  bomb(0,a);
+  return HUGE;
 }  
 
      
@@ -1414,6 +1419,7 @@
 
   return(-1);
   }
+  return(-1);
 }
 
 /*First, we check to see if the equation is even allowed.  To do that, solve 
@@ -2000,7 +2006,6 @@
   return (-832.);
 }
 */
-
 
 
 
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/fel.c b/fel.c
--- a/fel.c	2026-07-15 13:28:04
+++ b/fel.c	2026-07-15 19:09:23
@@ -1,7 +1,8 @@
 #include "main.h"
 #include "fel.h"
 #include <stdlib.h>
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <stdio.h>
 #include <math.h>
 #include <assert.h>
@@ -38,7 +39,7 @@
   return(Atime < Btime);
 } 
 
-int fel_sort(int debug) {
+void fel_sort(int debug) {
   for(int i=0;i<NP;i+=2) {
     int TheLeaf = (i / 2) + (NP/2) - 1; 
     int PNeighbor = (i % 2) ? (i-1) : (i+1);
@@ -54,11 +55,11 @@
 }
 
 
-int tree_resort( int a ) {
+void tree_resort( int a ) {
   int TheLeaf = (a / 2) + (NP/2) - 1; 
   int PNeighbor = (a % 2) ? (a-1) : (a+1);
   if (fel_min(fel[PNeighbor],fel[a]) && (fel[TheLeaf] == PNeighbor))
-    return 0;
+    return;
   else {
     fel[TheLeaf] = fel_min(a, PNeighbor) ? a : PNeighbor;
     fel_resort(TheLeaf);
@@ -85,7 +86,7 @@
   }
 }*/
 
-int fel_resort( int a ) {
+void fel_resort( int a ) {
   int TheLeaf = (a/2) + NP/2 -1;
   int PNeighbor = (a % 2) ? (a-1) : (a+1);
   int NewVal = (fel[TheLeaf] = fel_min(a,PNeighbor) ? a : PNeighbor);
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/fel.h b/fel.h
--- a/fel.h	2026-07-15 13:28:04
+++ b/fel.h	2026-07-15 19:09:23
@@ -1,7 +1,7 @@
 int fel_neighbor(int);
 int fel_parent(int);
 //inline int fel_min();
-int fel_sort(int);
-int tree_resort(int);
-int fel_resort(int);
+void fel_sort(int);
+void tree_resort(int);
+void fel_resort(int);
 //int fel_print();
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/files.c b/files.c
--- a/files.c	2026-07-15 13:28:04
+++ b/files.c	2026-07-15 13:28:04
@@ -1,6 +1,7 @@
 #include "files.h"
 #include <stdlib.h>
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <stdio.h>
 #include <math.h>
 #include "main.h"
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/main.h b/main.h
--- a/main.h	2026-07-15 13:28:04
+++ b/main.h	2026-07-15 19:09:47
@@ -4,6 +4,7 @@
 #define NMOV 2116
 /* sand.h -- data structures for the sand simulation experiment */
 
+#define PI 3.14159265358979323846
 #define DIMENSION 2 /*Dimensions in which the particles move*/
 #define QUASI 0
 #define SIDESMOVE 0
@@ -244,7 +245,7 @@
 #endif
 } P_DATA;
 
-typedef struct ParamStruct {
+struct ParamStruct {
 #if ((WALLFRICTION != 0) || (ROTATIONS == 1))
   double wmu;
 #endif
@@ -300,28 +301,28 @@
 void std_box();
 double get_ctime( int a );
 void cl_sort( void );
-int c_add( int, int, double );
-int cl_print();
-int new_collision();
+void c_add( int, int, double );
+void cl_print();
+void new_collision();
 int fel_change();
 int fel_neighbor( int );
 int fel_parent( int );
-int fel_sort( int );
-int tree_resort( int );
+void fel_sort( int );
+void tree_resort( int );
 int fel_min( int, int ); // returns index of minimum time fel entry
 int lel_kill( int );
-int lel_destroy( int );
+void lel_destroy( int, int );
 double cdetect( int, int );
 int c1detect(int, int, int);
 double c3detect(int,int);
 double cwdetect(int,int);
-int simulate(int step);
+void simulate(int step);
 int SetupXWindows(int,char**);
 int XMainLoop();
-int MainLoop();
+void MainLoop();
 void usage();
 int get_options(int,char**);
-int grid_destroy();
+void grid_destroy();
 int recalc_params();
 int validate(int);
 int cxdetect(int,int);
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/makefile b/makefile
--- a/makefile	2026-07-15 13:28:04
+++ b/makefile	2026-07-15 15:24:55
@@ -54,4 +54,3 @@
 	rm *.o
 
 
-
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/plist.c b/plist.c
--- a/plist.c	2026-07-15 13:28:04
+++ b/plist.c	2026-07-15 19:09:23
@@ -4,7 +4,8 @@
 #include "string.h"
 #include "collide.h"
 #include <math.h>
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <stdio.h>
 #include <stdlib.h>
 #include <assert.h>
@@ -919,7 +920,7 @@
 
 
 
-int pl_recalc() {
+void pl_recalc() {
   int sum = TheParams->nball + TheParams->nwall + TheParams->nvwall + TheParams->nstat + TheParams->njunk;
   if (sum > NP)
     exit(-sum);
@@ -1194,7 +1195,7 @@
 }
 
 
-int pl_print() {
+void pl_print() {
   int i;
     printf("\n                *** INITIAL PARTICLE LOCATIONS AND VELOCITIES ***\n\n");
   cout << "Gtime = " << Gtime << endl;
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/plist.h b/plist.h
--- a/plist.h	2026-07-15 13:28:04
+++ b/plist.h	2026-07-15 19:09:23
@@ -1,6 +1,6 @@
 void pl_init(void);
 void debug_planes(double*);
-int pl_print(void);
+void pl_print(void);
 void pty_print(int);
 #ifdef TONLY
 void slab_add(int);
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/stat.c b/stat.c
--- a/stat.c	2026-07-15 13:28:04
+++ b/stat.c	2026-07-15 19:09:23
@@ -1,5 +1,6 @@
 #include <stdlib.h>
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <stdio.h>
 #include "main.h"
 
@@ -9,7 +10,7 @@
 extern long NumWallColl;
 extern ParamStructPtr TheParams;
 
-int do_stats() {
+void do_stats() {
   /* calculate total energy in system */
   double Total_Energy = 0.0;
   for(int i=0;i<TheParams->nball;i++) 
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/stat.h b/stat.h
--- a/stat.h	2026-07-15 13:28:04
+++ b/stat.h	2026-07-15 19:09:23
@@ -1 +1 @@
-int do_stats();
+void do_stats();
diff -ruN --exclude=README.md --exclude=build.sh --exclude=PORT_CHANGES.diff a/xmain.c b/xmain.c
--- a/xmain.c	2026-07-15 13:28:04
+++ b/xmain.c	2026-07-15 19:09:47
@@ -29,7 +29,8 @@
 #include "files.h"
 #include <stdlib.h>
 #include <stdio.h>
-#include <iostream.h>
+#include <iostream>
+using namespace std;
 #include <math.h>
 #include <float.h>
 #include <time.h>
@@ -117,8 +118,8 @@
 double pzzflux[ZGSIZE];
 double pyzflux[ZGSIZE];
 double pzyflux[ZGSIZE];
-double loss[ZBSIZE];
-double gain[ZBSIZE];
+double loss[ZGSIZE];
+double gain[ZGSIZE];
 double vzbar[ZGSIZE];
 int listarray[NMOV];
 #if PERIODIC
@@ -151,14 +152,14 @@
     return(HUGE);
 }
 
-int grid_destroy() {
+void grid_destroy() {
   for(int x=0;x<XGSIZE;x++)
     for(int y=0;y<YGSIZE;y++)
       for(int z=0;z<ZGSIZE;z++)
 	TheGrid[x][y][z].removeall();
 }
 
-int new_collision() {
+void new_collision() {
   int ParA;
   int ParB;
   double CTime;
@@ -181,7 +182,7 @@
 
 //THIS
 
-int simulate(int step) {
+void simulate(int step) {
 //  pl_print();
 //  cl_print();
 //  fel_print();
@@ -406,7 +407,7 @@
 
 
 // simulation main loop -- text mode
-int MainLoop() {
+void MainLoop() {
 
     fprintf(stdout,"At the main Loop\n");
     int ok = 0;
@@ -440,7 +441,7 @@
 
 
 
-main(int argc, char **argv) {
+int main(int argc, char **argv) {
       TheParams->maxball = TheParams->nball = NMOV;
       TheParams->fball = FMOV;
       TheParams->lball = LMOV;
@@ -570,7 +571,5 @@
 //	the_gl_close();
     }
 }
-
-
 
 
